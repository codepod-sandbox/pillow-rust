[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[tool.maturin]
manifest-path = "crates/pil-imaging/Cargo.toml"
python-source = "python"
module-name = "PIL._imaging"

[tool.pytest.ini_options]
testpaths = ["tests/pillow_upstream"]
pythonpath = ["python"]
