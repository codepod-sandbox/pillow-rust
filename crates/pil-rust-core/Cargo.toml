[package]
name = "pil-rust-core"
version = "0.1.0"
edition = "2021"

[dependencies]
ab_glyph = "0.2.32"
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "gif", "bmp", "tiff", "webp"] }
