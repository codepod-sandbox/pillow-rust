#!/usr/bin/env bash
set -euo pipefail

echo "pre-push: building pil-imaging"
maturin develop -q

echo "pre-push: running pytest (failures are advisory)"
uv run pytest tests/pillow_upstream/ -q --tb=no || true
