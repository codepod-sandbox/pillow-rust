"""
PIL — Python Imaging Library (codepod WASM implementation)

Backed by the Rust `image` crate via _pil_native.

Usage::

    from PIL import Image        # imports the PIL.Image *module*
    img = Image.new('RGB', (100, 100))
"""
